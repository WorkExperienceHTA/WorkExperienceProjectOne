
namespace WorkExperienceProjectOne
{
    public class Questions
    {
        // Write code so that the methods return the expected results

        /// <summary>
        /// Orders the numbers in the input array from smallest to largest.
        /// </summary>
        public static int[] OrderNumbers(int[] input)
        {
            // This is a placeholder. Replace with actual logic to order the numbers.
            return [1, 2, 3];
        }

        /// <summary>
        /// Orders the numbers in the input array from smallest to largest and removes duplicates.
        /// </summary>
        public static int[] OrderNumbersAndRemoveDuplicates(int[] input)
        {
            // This is a placeholder. Replace with actual logic to order the numbers.
            return [1, 2, 3];
        }

        /// <summary>
        /// Returns the number of vowels (a, e, i, o, u) in the input string.
        /// </summary>
        public static int CountVowels(string input)
        {
            // This is a placeholder. Replace with actual logic to count vowels.
            return 0;
        }

        /// <summary>
        /// Returns true if the input string is a palindrome, false otherwise.
        /// </summary>
        public static bool IsPalindrome(string input)
        {
            // This is a placeholder. Replace with actual logic to check for palindromes.
            return true;
        }

        public static bool HasTwoSum(int[] input, int target)
        {
            // Placeholder; replace with actual logic
            return true;
        }

        /// <summary>
        /// Returns the length of the longest substring without repeating characters.
        /// </summary>
        public static int LengthOfLongestSubstring(string input)
        {
            // This is a placeholder. Replace with actual logic.
            return 0;
        }
    }
}
